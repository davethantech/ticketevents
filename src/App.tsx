import { useEffect, useMemo, useRef, useState } from "react";
import { AppProvider, useApp } from "./lib/store";
import { cx } from "./lib/utils";
import { Avatar, Badge, ToastProvider, useToast } from "./components/ui";
import { LogoMark, IGrid, IChart, ITicket, IUsers, IScan, IMail, IGlobe, IShield, IKey, IGear, IServer, ISearch, IChevronD, IBox } from "./components/icons";
import Dashboard from "./views/Dashboard";
import Reports from "./views/Reports";
import { EventsList, EventEditor, CreateEventModal } from "./views/Events";
import PublicEvent from "./views/PublicEvent";
import WidgetSetup from "./views/Widget";
import { OrdersView, AttendeesView } from "./views/OrdersAttendees";
import CheckIn from "./views/CheckIn";
import EventOperations from "./views/EventOperations";
import { TeamView, MessagesView, ApiPage, SelfHostView, SettingsView } from "./views/Platform";
import { signOutStaff, restoreStaffSession } from "./lib/auth";
import EntriaPublic from "./views/EntriaPublic";
import StaffLogin from "./views/StaffLogin";

function useHashRoute(): string { const [hash,setHash]=useState(()=>window.location.hash||"#/"); useEffect(()=>{const onHash=()=>setHash(window.location.hash||"#/");window.addEventListener("hashchange",onHash);return()=>window.removeEventListener("hashchange",onHash)},[]);return hash.replace(/^#/,'')||'/'; }
const navigate=(to:string)=>{window.location.hash=to;window.scrollTo({top:0})};
const NAV=[{label:"Overview",items:[{to:"/dashboard",label:"Dashboard",icon:IGrid},{to:"/reports",label:"Reports",icon:IChart}]},{label:"Manage",items:[{to:"/events",label:"Events",icon:ITicket},{to:"/operations",label:"Event control",icon:IScan},{to:"/orders",label:"Orders",icon:IUsers},{to:"/attendees",label:"Attendees",icon:IUsers},{to:"/checkin",label:"Check-in",icon:IScan},{to:"/messages",label:"Messages",icon:IMail},{to:"/widget",label:"Embed widget",icon:IGlobe}]},{label:"Platform",items:[{to:"/team",label:"Team",icon:IShield},{to:"/api",label:"API",icon:IKey},{to:"/settings",label:"Settings",icon:IGear},{to:"/selfhost",label:"Self-host",icon:IServer}]}];
const TITLES:Array<[string,string]>=[["/dashboard","Dashboard"],["/events","Events"],["/operations","Event control"],["/orders","Orders"],["/attendees","Attendees"],["/checkin","Check-in"],["/messages","Messages"],["/widget","Embed widget"],["/team","Team"],["/api","API"],["/settings","Settings"],["/selfhost","Self-hosting"],["/reports","Reports"]];

function StaffShell(){
 const {db,user}=useApp(); const toast=useToast(); const route=useHashRoute(); const [pathRaw]=route.split("?"); const path=pathRaw||"/dashboard"; const seg=path.split("/").filter(Boolean); const [creating,setCreating]=useState(false); const [userMenu,setUserMenu]=useState(false); const [q,setQ]=useState(""); const [searchOpen,setSearchOpen]=useState(false); const searchRef=useRef<HTMLDivElement>(null);
 useEffect(()=>{const onDoc=(e:MouseEvent)=>{if(searchRef.current&&!searchRef.current.contains(e.target as Node))setSearchOpen(false)};document.addEventListener("mousedown",onDoc);return()=>document.removeEventListener("mousedown",onDoc)},[]);
 const results=useMemo(()=>{const n=q.trim().toLowerCase();if(!n)return{events:[],people:[] as typeof db.attendees};return{events:db.events.filter(e=>e.name.toLowerCase().includes(n)).slice(0,4),people:db.attendees.filter(a=>[a.name,a.email,a.code].some(s=>s.toLowerCase().includes(n))).slice(0,5)}},[q,db]);
 if(seg[0]==="embed")return <div className="min-h-screen bg-bg"><PublicEvent slug={seg[1]??""} embedded/></div>;
 if(seg[0]==="page")return <div className="min-h-screen bg-bg"><PublicEvent slug={seg[1]??""} onNavigateHome={()=>navigate("/")}/></div>;
 const title=TITLES.find(([p])=>path===p||path.startsWith(p+"/"))?.[1]??"Entria"; const isActive=(to:string)=>path===to||path.startsWith(to+"/");
 return <div className="flex min-h-screen"><aside className="fixed inset-y-0 left-0 z-40 hidden w-[228px] flex-col border-r border-nightline bg-night text-[#e7eae4] lg:flex"><button onClick={()=>navigate("/dashboard")} className="focus-ring flex items-center gap-2.5 px-5 pb-5 pt-6 text-left"><LogoMark size={30}/><span><span className="block font-display text-[17px] font-bold leading-none tracking-tight text-white">Entria</span><span className="code-pill mt-0.5 block text-[9.5px] uppercase tracking-[0.18em] text-[#8d968e]">event experiences</span></span></button><nav className="flex-1 space-y-5 overflow-y-auto px-3 pb-4">{NAV.map(g=><div key={g.label}><p className="code-pill px-2.5 pb-1.5 text-[9.5px] uppercase tracking-[.18em] text-[#5f6a62]">{g.label}</p><div className="space-y-0.5">{g.items.map(it=>{const Icon=it.icon;const active=isActive(it.to);return <button key={it.to} onClick={()=>navigate(it.to)} className={cx("focus-ring group relative flex w-full items-center gap-2.5 rounded-lg px-2.5 py-2 text-[13px] font-semibold transition-all duration-150",active?"bg-night2 text-white":"text-[#9aa29a] hover:bg-night2/60 hover:text-white")}>{active&&<span className="absolute left-0 top-1/2 h-4 w-[3px] -translate-y-1/2 rounded-r bg-flame"/>}<Icon size={16} className={cx(active?"text-flame2":"text-[#5f6a62] group-hover:text-[#9aa29a]")}/>{it.label}{it.to==="/checkin"&&db.events.some(e=>e.status==="live")&&<span className="ml-auto h-1.5 w-1.5 rounded-full bg-flame blinker"/>}</button>})}</div></div>)}</nav><div className="border-t border-nightline p-3"><button onClick={()=>navigate("/selfhost")} className="focus-ring stripe-hatch-dark flex w-full items-center gap-2.5 rounded-lg bg-night2 px-3 py-2.5 text-left"><IBox size={16} className="text-teal2"/><span className="flex-1"><span className="block text-[12px] font-bold text-white">Runs on your box</span><span className="block text-[10.5px] text-[#8d968e]">PWA · self-hosted</span></span><span className="h-2 w-2 rounded-full bg-ok"/></button></div></aside>
 <div className="min-w-0 flex-1 lg:pl-[228px]"><header className="sticky top-0 z-30 border-b border-line bg-bg/85 backdrop-blur"><div className="flex items-center gap-3 px-5 py-3 lg:px-8"><button onClick={()=>navigate("/dashboard")} className="focus-ring lg:hidden"><LogoMark size={26}/></button><p className="code-pill hidden text-[11px] uppercase tracking-widest text-faint sm:block">{db.settings.org}</p><span className="hidden text-line2 sm:block">/</span><h2 className="font-display text-[15px] font-bold tracking-tight">{title}</h2><div ref={searchRef} className="relative ml-auto w-full max-w-[300px]"><ISearch size={14} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-faint"/><input value={q} onChange={e=>{setQ(e.target.value);setSearchOpen(true)}} onFocus={()=>setSearchOpen(true)} placeholder="Search events, people, codes…" className="focus-ring h-9 w-full rounded-lg border border-line2 bg-white pl-9 pr-3 text-[12.5px] placeholder:text-faint"/>{searchOpen&&q.trim()&&<div className="pop-in absolute left-0 right-0 top-11 overflow-hidden rounded-xl border border-line bg-paper shadow-[var(--shadow-pop)]">{results.events.map(e=><button key={e.id} onClick={()=>{navigate(`/events/${e.id}`);setQ("");setSearchOpen(false)}} className="flex w-full items-center gap-2.5 px-4 py-2.5 text-left hover:bg-white"><span className="h-6 w-6 shrink-0 overflow-hidden rounded-md"><img src={e.cover} alt="" className="h-full w-full object-cover"/></span><span className="truncate text-[13px] font-semibold">{e.name}</span><Badge className="ml-auto">{e.status}</Badge></button>)}{results.people.map(a=><button key={a.id} onClick={()=>{navigate("/attendees");setQ("");setSearchOpen(false)}} className="flex w-full items-center gap-2.5 px-4 py-2.5 text-left hover:bg-white"><Avatar name={a.name} color="#0e6b60" size={24}/><span className="min-w-0"><span className="block truncate text-[13px] font-semibold">{a.name}</span><span className="code-pill block text-[10.5px] text-mut">{a.code} · {a.ticketName}</span></span></button>)}</div>}</div><button onClick={()=>setCreating(true)} className="focus-ring hidden rounded-lg bg-flame px-3.5 py-2 text-[12px] font-bold text-white sm:block">+ New event</button><div className="relative"><button onClick={()=>setUserMenu(!userMenu)} className="focus-ring flex items-center gap-2 rounded-lg border border-line bg-white py-1 pl-1 pr-2"><Avatar name={user.name} color={user.color} size={28}/><span className="hidden text-left sm:block"><span className="block text-[12px] font-bold">{user.name.split(" ")[0]}</span><span className="code-pill block text-[9.5px] uppercase tracking-wider text-faint">{user.role}</span></span><IChevronD size={13} className={cx("text-faint",userMenu&&"rotate-180")}/></button>{userMenu&&<><div className="fixed inset-0 z-40" onClick={()=>setUserMenu(false)}/><div className="pop-in absolute right-0 top-12 z-50 w-60 overflow-hidden rounded-xl border border-line bg-paper shadow-[var(--shadow-pop)]"><p className="code-pill border-b border-line px-3.5 py-2 text-[10px] uppercase tracking-widest text-faint">Staff session</p><div className="px-3.5 py-3"><strong className="block text-[12.5px]">{user.name}</strong><span className="text-[10.5px] capitalize text-mut">{user.role}</span></div><button onClick={async()=>{await signOutStaff();window.location.hash="/staff/login";window.location.reload()}} className="w-full border-t border-line px-3.5 py-3 text-left text-[12px] font-bold text-bad hover:bg-baddim">Sign out of staff portal</button></div></>}</div></div></header><main key={route} className="page-in">{path==="/dashboard"&&<Dashboard navigate={navigate}/>} {path==="/events"&&<EventsList navigate={navigate}/>} {seg[0]==="events"&&seg[1]&&<EventEditor id={seg[1]} tab={seg[2]??"details"} navigate={navigate}/>} {seg[0]==="operations"&&<EventOperations id={seg[1]} navigate={navigate}/>} {path==="/orders"&&<OrdersView/>} {path==="/attendees"&&<AttendeesView/>} {path==="/checkin"&&<CheckIn/>} {path==="/messages"&&<MessagesView/>} {path==="/widget"&&<WidgetSetup/>} {path==="/team"&&<TeamView/>} {path==="/api"&&<ApiPage/>} {path==="/settings"&&<SettingsView/>} {path==="/selfhost"&&<SelfHostView/>} {path==="/reports"&&<Reports/>}</main></div>{creating&&<CreateEventModal onClose={()=>setCreating(false)}/>}</div>
}

export default function App(){
 const route=useHashRoute(); const [path]=route.split("?");
 if(path==="/"||path==="") return <EntriaPublic/>;
 if(path==="/staff/login") return <StaffLogin onBack={()=>navigate("/")}/>;
 return <AppProvider><ToastProvider><ProtectedStaffShell/></ToastProvider></AppProvider>;
}

function ProtectedStaffShell(){
 const [ready,setReady]=useState<boolean|null>(null);
 useEffect(()=>{restoreStaffSession().then(setReady)},[]);
 if(ready===null) return <div className="grid min-h-screen place-items-center bg-bg"><div className="rounded-full border border-line bg-white px-4 py-2 text-xs font-semibold text-mut">Checking staff session…</div></div>;
 if(!ready){ window.location.hash="/staff/login"; return <div className="min-h-screen bg-bg"/>; }
 return <StaffShell/>;
}
